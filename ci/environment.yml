name: sparse-dev
channels:
  - numba
  - conda-forge
  - nodefaults
dependencies:
  - python
  - pip
  - numpy
  - numba
  - scipy
  - dask
  - pytest
  - pytest-cov
  - pytest-xdist
  - pip:
    - finch-tensor>=0.2.2
    - finch-mlir>=0.0.2
    - pytest-codspeed
